# LX Music Desktop — 播放详情页动态封面（Apple Music）改动汇总

> 项目：lx-music-desktop v2.0.1（工作区 `F:\player\lx-music-desktop-master\lx-music-desktop-master`）
> 日期：2026-08-19
> 说明：本文档仅汇总本次「Apple Music 动态封面」功能相关的改动。工作区中还包含大量**既有未提交改动**（网易云每日推荐、native-core 后端开关等），它们不属于本次改动，已在文末单独标注，请勿混淆。

---

## 一、功能概述

在播放详情页（点击浮岛放大的二级界面）支持展示 **Apple Music 动态封面**，并新增两类设置入口：

1. **外观设置 → 封面设置**：静态封面 / 动态封面 单选切换（默认静态）。
2. **模糊背景 → 动态封面开关**：
   - 普通播放详情页的模糊背景（`背景模糊度` 横条右侧）
   - 沉浸模式（`设置 → 沉浸模式背景` 模糊背景横条右侧）

当在播放详情页搜索到当前歌曲有 Apple Music 动态封面时，会弹窗询问「是否切换为动态封面」，确认后自动开启动态封面。

---

## 二、新增文件（4 个）

### 1. `src/renderer/utils/appleDynamicCover/index.ts`（333 行）
Apple Music 动态封面核心服务。

- **搜索**：调用 `amp-api.music.apple.com/v1/catalog/{storefront}/search`（携带 Bearer token），按 `albums,songs` 两类搜索，候选 storefront 依次尝试 `cn → hk → tw → us → gb → jp → de → fr`，兼容国内网络。
- **匹配**：基于歌曲名 / 歌手 / 专辑名做归一化（去括号、小写）与 LCS 相似度评分，选出最佳专辑 id。
- **获取动态封面**：请求 `albums/{id}?extend=editorialVideo`，从 `attributes.editorialVideo` 中按顺序取 `motionDetailSquare → motionSquareVideo1x1 → motionDetailTall` 的 `video`（m3u8 地址）与 `previewFrame`（可作 poster）。
- **HLS 变体选择**：解析 master 播放列表，**优先 H.264(AVC) 变体**（Chromium/Electron 原生 HLS 对 HEVC 依赖硬件解，AVC 最稳）；若全为 HEVC 则取中档分辨率。
- **缓存**：按 `歌曲名|歌手|专辑` 内存缓存（上限 50 条），并发请求去重（pending Map）+ 请求串行化。

### 2. `src/renderer/utils/appleDynamicCover/token.ts`（135 行）
Apple Music 网页版媒体 token（WebPlayKid JWT）获取与缓存。

- 访问 `music.apple.com` 公开专辑页 → 提取主 JS bundle 路径 → 拉取 JS → 正则提取 JWT → 解码 header，筛选 `kid === 'WebPlayKid'`。
- 解析 JWT payload 的 `exp`，提前 10 分钟视为过期；`localStorage` 持久化缓存（key：`lx_apple_web_token`）。
- 鉴权失败（401/403）时自动清除缓存 token，下次重取。

### 3. `src/renderer/utils/appleDynamicCover/hls.ts`（82 行）
HLS master 播放列表解析工具。

- `parseMasterPlaylist(content)`：解析 `#EXT-X-STREAM-INF` 变体的 `CODECS` / `RESOLUTION` / `BANDWIDTH` / URI。
- `pickBestVariant()`：优先 AVC，其次 HEVC 取中档分辨率。
- `resolveVariantMediaUrl()`：将相对 URI 解析为完整地址。

### 4. `src/renderer/store/player/dynamicCover.ts`（87 行）
动态封面全局状态 store（Vue `ref`）。

- 导出 `dynamicCoverUrl`、`dynamicCoverPoster`、`dynamicCoverState`（`idle|loading|ready|error`）。
- `loadDynamicCover(info)`：按歌曲 id（或 名|歌手|专辑）去重加载，成功/失败设置状态。
- `resetDynamicCover()`：重置状态（停止播放时）。
- `clearDynamicCoverCache()`：清空缓存并重置。

---

## 三、修改文件（10 个）

### 1. `src/common/defaultSetting.ts`（+7 行）
新增 3 个设置项默认值：

```ts
'playDetail.coverType': 'static',                        // 封面类型：静态/动态
'playDetail.backgroundUseDynamicCover': false,           // 详情页模糊背景使用动态封面
'playDetail.immersiveBackgroundUseDynamicCover': false,  // 沉浸模式模糊背景使用动态封面
```

> 注意：diff 中还包含工作区原有的 `backend.metadata` / `backend.metadataWrite` / `backend.artwork` 三个键，**不是本次改动**。

### 2. `src/common/types/app_setting.d.ts`（+20 行）
为上述 3 个新设置项补充类型声明：

```ts
'playDetail.coverType': 'static' | 'dynamic'
'playDetail.backgroundUseDynamicCover': boolean
'playDetail.immersiveBackgroundUseDynamicCover': boolean
```

> 同样，diff 中工作区原有的 `backend.*` 类型声明不属于本次改动。

### 3. `src/renderer/views/Setting/components/SettingAppearance.vue`（+39 行）
「外观设置」页新增 **封面设置** 区块：

- 模板：`h3#appearance_cover_setting` + 说明文字 + 两个单选卡片（`static` / `dynamic`），复用现有 `viewOptions` / `viewOption` 样式与 `base-checkbox(need)` 单选组。
- 脚本：新增 `coverStyles` 数组，文案经 `window.i18n.t()` 取翻译。
- 样式：新增 `.coverTip`。

### 4. `src/renderer/views/Setting/components/SettingPlayDetail.vue`（+26 行）
「播放详情页背景 → 模糊」控制行新增 **动态封面开关**：

- 在 `背景模糊度` 横条右侧新增 `span.blurDynamicOption`，内含 `base-checkbox`（`playDetail.backgroundUseDynamicCover`）+ 说明文字。
- 样式调整：`.blurControl` 改为可换行（`flex-wrap`）、max-width 提到 720px、滑杆 `min-width: 180px`；新增 `.blurDynamicOption` 样式。

### 5. `src/renderer/components/layout/PlayDetail/ImmersiveStylePanel.vue`（+45 行）
沉浸模式样式面板「背景 → 模糊」新增 **动态封面开关**：

- 模糊横条包进 `div.blurRow`（label + slider 同行两端对齐）。
- 下方新增 `div.dynamicOption`：`base-checkbox`（`playDetail.immersiveBackgroundUseDynamicCover`）+ 说明文字。
- 新增 `.blurRow` / `.dynamicOption` 样式。

### 6. `src/renderer/components/layout/PlayDetail/index.vue`（+145 行）
播放详情页主体，核心改动：

- **模板**：
  - 大封面 `button.artworkWrap` 内：`showDynamicCoverArtwork` 时渲染 `<video>`（muted/loop/autoplay/playsinline，`poster` 用动态封面预览帧或静态图），否则回退原 `<img>`。
  - 模糊背景 `div.bgBlur` 内：`showBlurDynamicCover` 时渲染 `<video>`（同样属性），实现动态封面作模糊背景。
- **脚本**：
  - 新增状态 `artworkVideoFailed` / `bgBlurVideoFailed`（视频加载失败自动回退静态）、`artworkVideoRef` / `bgBlurVideoRef`。
  - 新增 `showDynamicCoverArtwork` / `showBlurDynamicCover` computed。
  - **惰性加载**：`tryLoadDynamicCover()` 在**播放详情页打开时**才检查并加载动态封面（延迟 800ms），并**弹窗询问**（`dialog.confirm`）「检测到这首歌支持 Apple Music 动态封面，是否切换为动态封面？」，确认后 `updateSetting({ 'playDetail.coverType': 'dynamic' })`。每首歌只询问一次（`promptedMusicId` 去重）。
  - 切歌时重置视频失败标志；播放容器从隐藏切可见时手动 `play()` 确保 autoplay 生效。
  - 监听设置项变化：开启任意动态相关开关且未加载过时自动加载。
- **样式**：新增 `.bgBlurVideo`（铺满 + `object-fit: cover`）。

### 7. `src/renderer/components/layout/PlayDetail/ImmersiveLyrics.vue`（+35 行）
沉浸模式歌词页：

- 模糊背景 `div.blurBackground` 内新增 `<video>`，条件 `showBlurDynamicCover`（`immersiveBackgroundUseDynamicCover` 开启 + 已加载动态封面 + 未失败）。
- 新增 `blurVideoFailed` 状态，切歌时重置。
- 新增 `.blurBackgroundVideo` 样式。

### 8. `src/lang/zh-cn.json`（+11 条新键）
本次新增翻译键：

```jsonc
"setting__cover": "封面设置",
"setting__cover_tip": "选择播放详情页展示的封面类型，动态封面来自 Apple Music 并需联网获取",
"setting__cover_static": "静态封面",
"setting__cover_static_desc": "使用歌曲专辑的静态封面图",
"setting__cover_dynamic": "动态封面",
"setting__cover_dynamic_desc": "使用 Apple Music 动态封面，未获取到时自动回退静态封面",
"setting__play_detail_background_dynamic_cover": "动态封面",
"setting__play_detail_background_dynamic_cover_tip": "开启后使用动态封面作为模糊背景，未获取到时回退专辑封面",
"setting__play_detail_immersive_background_use_dynamic": "动态封面背景",
"setting__play_detail_immersive_background_use_dynamic_tip": "开启后使用动态封面作为沉浸模式的模糊背景，未获取到时回退专辑封面",
"setting__play_detail_dynamic_cover_prompt": "检测到这首歌支持 Apple Music 动态封面，是否切换为动态封面？",
"setting__play_detail_dynamic_cover_prompt_yes": "切换",
"setting__play_detail_dynamic_cover_prompt_no": "暂不"
```

> lang diff 中还包含工作区原有的网易云每日推荐等键，**不属于本次改动**。

### 9. `src/lang/zh-tw.json`（+11 条新键）
繁体中文对应翻译（内容同上，繁中措辞）。

### 10. `src/lang/en-us.json`（+11 条新键）
英文对应翻译。

---

## 四、验证情况

| 验证项 | 结果 |
| --- | --- |
| `npm run typecheck`（common + main + renderer 三端 tsc） | ✅ 通过，0 错误 |
| Vue SFC 语法（pug 模板 + script，5 个改动组件） | ✅ 全部通过 |
| 三份语言文件 key 一致性（zh-cn / zh-tw / en-us） | ✅ 900 key 完全对齐 |
| Apple 接口端到端（真实数据：搜索 → editorialVideo → HLS 变体 → 分片可访问） | ✅ 通过（Positions / Ariana Grande → AVC 1080x1080） |
| Webpack 完整构建 | ⚠️ 沙箱性能限制超时，未能完成；但 tsc 全量类型检查与 SFC 语法均通过，建议本机 `npm run dev` 或 `npm run build:renderer` 验证 |

---

## 五、已知限制与注意事项

1. **动态封面获取需联网**访问 Apple 接口；获取失败/无动态封面时自动回退静态封面，不影响原有功能。
2. **HLS 播放依赖 Chromium 原生支持**（Electron 37 / Chromium 140 已支持原生 HLS）。动态封面默认优先 AVC 变体，兼容性最佳；若系统无法解码（极端情况），`<video>` 触发 `error` 事件自动回退静态封面。
3. **弹窗触发时机**：仅当**播放详情页打开**时触发惰性检查（避免应用启动时立即联网拖慢初始化）；且仅当当前 `coverType != 'dynamic'` 且该歌曲尚未询问过时弹窗。若已开启动态封面，则直接展示不弹窗。
4. **token 时效**：Apple 的 WebPlayKid JWT 会过期，本地缓存过期后会自动从 music.apple.com 重新获取。

---

## 六、工作区既有改动（非本次，勿混淆）

以下改动在 `git status` 中同样显示为已修改/未跟踪，但**与本次动态封面功能无关**（来源：工作区原有开发内容）：

- `src/renderer/utils/musicSdk/wy/account.js`（新增，网易云账号/每日推荐）
- `src/renderer/utils/musicSdk/wy/index.js`、`wy/utils/index.js`（网易云 account 接入）
- `src/renderer/utils/musicInfo.ts`（行尾符差异）
- `src/common/defaultSetting.ts` / `app_setting.d.ts` 中的 `backend.metadata*` / `backend.artwork` 键（native-core 后端开关）
- `src/lang/*.json` 中的网易云每日推荐、播放队列、浮岛等翻译键
- `native-core/`（Rust 项目，工作区自带）
- 构建配置 `build-config/*`、`package.json` 等（工作区自带调整）

> 如果你遇到「无法初始化」问题，请先确认：① 是否修改过 `src/renderer/utils/musicSdk/wy/account.js` 相关（工作区原有改动可能引用尚未合入的模块）；② 构建产物是否是最新（沙箱中 webpack 构建超时未完成）。本次动态封面改动本身不涉及启动初始化链路，模块加载无副作用。

---

## 七、2026-08-19 整合完成记录

本汇总描述的改动已纳入 `hybrid-native` 的 Stage 1 收尾。整合时补齐了 HLS 父级相对 URL、token 并发与主动失效、跨 storefront 查询、同曲请求合并，以及详情页打开时的惰性探测。动态封面失败仍只回退静态封面，不改变播放后端。

最终验证：动态封面专项 5/5、前端整合专项合计 13/13、typecheck、lint、生产构建和 Renderer smoke 均通过。上文“Webpack 未完成”和“未 commit”是暂停时的历史状态，不再代表当前结果。
