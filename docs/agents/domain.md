# Domain Documentation

This is a single-context repository.

- Repository context: `CONTEXT.md` at the repository root when present.
- Architecture decisions: `docs/adr/`.
- Agents should read these documents before making broad architectural changes.
- If either file or directory does not exist, create it when the task requires durable
  project context or an architecture decision record.
