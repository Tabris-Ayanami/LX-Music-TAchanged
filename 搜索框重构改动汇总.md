# 顶栏搜索框 UI 重构改动汇总

> 重构目标：将顶栏搜索框从普通输入框改造成 **gooey 液态分离胶囊搜索框**，视觉参考 GooeySearchBar（framer-motion 风格）。
> 涉及 2 个文件，均为本次改动，未触碰其它模块。

---

## 改动文件清单

| 文件 | 改动性质 |
|------|---------|
| `src/renderer/components/material/SearchInput.vue` | 整体重写（核心组件） |
| `src/renderer/components/layout/Toolbar/SearchInput.vue` | 逻辑增强（父组件，联动子组件新 prop） |

---

## 一、`src/renderer/components/material/SearchInput.vue`（核心组件，整体重写）

### 1. 交互形态：折叠胶囊 → 点击展开

- **折叠态**：一个胶囊型搜索框（默认宽 150px），内部左侧为"搜索"占位文字，右侧为一个小号搜索图标（`.innerIcon`）。点击或快捷键聚焦时展开。
- **展开态**：胶囊撑满容器宽度，内部切换为输入框 + 清空按钮；右侧浮出主色圆形搜索按钮。
- 失焦（blur）后自动折叠回胶囊态，保留已输入文字（折叠态胶囊显示当前关键词）。

### 2. gooey 液态分离动画

- 在组合层 `.goo` 上应用 SVG `filter: url(#lx-search-goo)`（feGaussianBlur + feColorMatrix + feComposite），实现胶囊与圆球在分离过程中的"液态拉丝"粘连效果。
- **分离过程**（关键修复）：圆球初始状态为 `transform: translateX(-58px) scale(.4)` + `opacity: 0`，物理上藏在胶囊内部右端；展开时过渡到 `translateX(0) scale(1)`。圆球 **opacity 快速显形（0.15s）→ 再随位移/间距缓慢分离（0.55s）**，配合 gooey 滤镜形成"从胶囊内部滑出并拉丝分离"的连续动画，而非凭空出现。
- 折叠时圆球完全不可见（opacity 0、pointer-events none），**初始态没有外部圆球**，只有胶囊内部右侧的小图标。
- 展开终态间距 `gap`：small = 16px，默认 = 18px，确保圆球与胶囊**完全脱离 gooey 阈值**，不残留粘连/竖线。

### 3. 视觉样式

- **高度**：small（顶栏）= 43px；默认 = 50px。圆球：small = 38px，默认 = 46px。
- **内凹（sunken）效果**：胶囊使用 `inset` 阴影制造"嵌进顶栏表面"的凹陷感——
  - 浅色：`inset 0 2px 5px`（顶部内侧暗影）+ `inset 0 -1px 1px rgba(255,255,255,.75)`（底部内侧高光）；
  - 深色：`inset 0 2px 5px rgba(0,0,0,.4)` + `inset 0 -1px 1px rgba(255,255,255,.05)`；
  - 聚焦态凹陷加深（`inset 0 3px 7px`）。
- **聚焦指示**：仅淡主色描边 + 内部 inset 微光，**不使用主色外发光**（避免 gooey 滤镜下在胶囊右缘锐化出主色竖线 artifact）。
- **光标位置**：输入框 `padding: 0 14px`（small 为 `0 12px`），较初版右移 2px。
- **圆球**：主色渐变圆钮（`linear-gradient(135deg, var(--color-primary), ...)`），带主色投影 + 内高光；hover 轻微放大，active 收缩。请求中显示旋转 loading 图标。

### 4. 联想下拉面板

- 独立浮层面板，**位于 gooey 滤镜容器之外**，保持锐利描边、不受液态滤镜影响。
- 背景与搜索框同底色（`--search-panel-bg`，浅色近白 / 深色冷炭），四周完整描边 + 浮起阴影，形成可读的一体式面板。
- 面板宽度与胶囊对齐（`width: calc(100% - 圆球 - 间距)`）。
- **联想项动画**：使用 `<transition-group>`，每次结果更新时项从底部 `translateY(10px) scale(.96) blur(6px)` 级联浮现（`transitionDelay: index * 45ms`），对应参考组件 framer-motion 的结果项动画。

### 5. 保留的既有逻辑（未改动）

- props：`placeholder / list / visibleList / modelValue / big / small`，新增 `isLoading`。
- emits：`update:modelValue / event`（action: `focus / blur / submit / listClick / change`）。
- 快捷键聚焦（`HOTKEY_COMMON.focusSearchInput`）、右键粘贴剪贴板、上下键选择联想项、清空按钮、`search.isFocusSearchBox` 设置项等逻辑全部保留。
- 关键细节：点击胶囊空白区聚焦输入框；展开后输入框/圆球可交互（`pointer-events` 切换）；折叠时胶囊作为可访问按钮（`role="button"`、`aria-expanded`）。

---

## 二、`src/renderer/components/layout/Toolbar/SearchInput.vue`（父组件，逻辑增强）

### 1. 新增 `isLoading` 状态

- 新增 `const isLoading = ref(false)`，并传给子组件 `:is-loading="isLoading"`。
- 在 `tipSearch` 中：请求发起时置 `true`，成功/失败/取消时置 `false`；`blur` 时复位。
- 用途：让子组件搜索圆球在联想请求进行中显示**旋转 loading 图标**。

### 2. 新增 i18n 占位文案

- 引入 `useI18n`，`const placeholder = t('search')`（"搜索"），通过 `:placeholder` 传入子组件，替代原英文硬编码 `'Search for something...'`。

### 3. 模板调整

- 子组件调用从单行改为多行，新增 `:is-loading` 与 `:placeholder` 绑定。

### 4. 未改动的逻辑

- 路由跳转（/search、/local）、联想词请求（`music[source].tipSearch.search`）、搜索历史/设置项联动、防抖、请求竞态控制（`tipRequestId`）等全部保留。

---

## 三、设计要点回顾（对应需求）

| 需求 | 实现 |
|------|------|
| 圆球从搜索框液态分离 | gooey filter + translateX/scale/gap 时序过渡，球从胶囊内部滑出拉丝分离 |
| 初始态无外部圆球 | 折叠态只有胶囊内部右侧小图标，外部圆球 opacity 0 隐藏 |
| 消除主色竖线 | 聚焦不用主色外发光；终态 gap 足够大完全脱离 gooey 阈值 |
| 下拉面板可读性 | 面板与搜索框同底色、四周描边、浮起阴影、不受滤镜影响 |
| 内凹嵌顶栏效果 | 胶囊 inset 顶部暗影 + 底部高光，聚焦加深 |
| 高度 43px | small 胶囊 43px，圆球 38px |

---

## 四、备注

- 未安装任何新依赖（gooey 为 SVG filter 原生实现；不依赖 framer-motion / clsx）。
- 浅色 / 深色两套配色均沿用应用现有 `--shell-*` token，未引入新色板。

---

## 五、2026-08-19 整合完成记录

搜索框重构已纳入 `hybrid-native`。整合时补齐折叠态的 Enter/Space 键盘入口、43px 紧凑容器命中区和 reduced-motion；原有搜索、防抖、竞态与路由语义保持不变。相关前端专项回归、typecheck、lint、生产构建及 Renderer smoke 已通过。
